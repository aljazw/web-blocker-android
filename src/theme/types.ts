export type Theme = {
    mode: 'light' | 'dark';
    colors: {
        background: string;
        text: string;
        primaryRed: string,    
        primaryBlue: string,
        card: string;
        border: string;
    };
};