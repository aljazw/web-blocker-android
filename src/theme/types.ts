export type Theme = {
    mode: 'light' | 'dark';
    colors: {
        background: string;
        text: string;
        primaryRed: string,    
        primaryBlue: string,
        primaryGreen: string,
        card: string;
        border: string;
    };
};